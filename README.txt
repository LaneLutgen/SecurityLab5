README

To run the program, supply arguments in the following order:

java ids <pcap file> <policy file>

The program will output to the console if an error is found, otherwise,
it will not print anything.